import { FilterTorneoPipe } from './filter-torneo.pipe';

describe('FilterTorneoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTorneoPipe();
    expect(pipe).toBeTruthy();
  });
});
